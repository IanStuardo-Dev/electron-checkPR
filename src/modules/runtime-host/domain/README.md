# Runtime Host Domain

Esta capa queda reservada para entidades, value objects y reglas puras de dominio del proceso principal.

Actualmente el modulo `runtime-host` concentra logica de orquestacion y transporte, por lo que no requiere entidades de dominio explicitas todavia.
